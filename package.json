{
  "name": "json-tracing-repro",
  "version": "0.1.0",
  "private": true,
  "workspaces": ["packages/json-logger", "site"],
  "scripts": {
    "postinstall": "yarn workspace json-logger build",
    "dev": "yarn workspace site dev",
    "build": "yarn workspace json-logger build && yarn workspace site build",
    "start": "yarn workspace site start"
  }
}
